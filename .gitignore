# AI 辅助工具(skills)的配置,不属于 app 本身,不上传到 GitHub
.claude/
.agents/
skills-lock.json
CLAUDE.md

# 软著申请材料(含源码摘录/个人身份证件,绝不进 git)
.copyright-apply/

# docs/ 下大部分是本地 AI 工作笔记(交接 handover、agent 工作流),不上 git;
# 但【架构决策 ADR】是仓库该有的东西,放开 docs/adr/ 进版本控制。
docs/*
!docs/adr/
# 隐私政策要发布到 GitHub Pages(Google Play 要求公开 URL),必须进 git。
!docs/privacy-policy.md
!docs/privacy-policy.html
# .nojekyll 告诉 Pages 跳过 Jekyll 原样输出(否则隐私政策 .html 被过滤成 404)。
!docs/.nojekyll

# node 依赖(以后如果用得到)
node_modules/
