# AI 辅助工具(skills)的配置,不属于 app 本身,不上传到 GitHub
.claude/
.agents/
skills-lock.json
CLAUDE.md

# docs/ 下大部分是本地 AI 工作笔记(交接 handover、agent 工作流),不上 git;
# 但【架构决策 ADR】是仓库该有的东西,放开 docs/adr/ 进版本控制。
docs/*
!docs/adr/

# node 依赖(以后如果用得到)
node_modules/
