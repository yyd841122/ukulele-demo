// 全项目音频走统一的采样率:开麦(MicCapture)产样本、测音高(PitchDetector.detect)吃样本,
// 两边必须用同一个数——频率 = 采样率 / 周期样本数,不一致会静默算错音高还很难查。
// 集中到一个常量,改一处就够,杜绝两个文件各写一个 44100、改一边忘改另一边。

/// 麦克风采样 + 基频检测共用的采样率(Hz)。
const int kAudioSampleRate = 44100;
